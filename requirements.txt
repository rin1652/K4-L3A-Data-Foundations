# Core requirements only. These are sufficient for every required checkpoint —
# all 42 tests run on MockEmbedder, no real embedding backend needed.
pytest>=8.0.0
python-dotenv>=1.2.1

# Want a real embedding backend for benchmark quality (optional, not graded)?
#   Local, free, no API key : pip install -r requirements-local.txt
#   OpenAI                  : pip install openai
#   Gemini (free tier)      : pip install google-genai
# See README.md "Tuy Chon Mo Hinh Nhung" or Phu luc B in the lab doc.
